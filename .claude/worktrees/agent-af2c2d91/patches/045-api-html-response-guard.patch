diff --git a/server/index.js b/server/index.js
index 4a2419d..2efaa50 100755
--- a/server/index.js
+++ b/server/index.js
@@ -1314,6 +1314,12 @@ app.get('/api/projects/:projectId/sessions/:sessionId/token-usage', authenticate
     }
 });

+// Return JSON 404 for unmatched /api/* routes so the SPA catch-all below
+// never serves index.html for API requests (which causes JSON-parse errors).
+app.all('/api/*', (req, res) => {
+    res.status(404).json({ error: 'Not found' });
+});
+
 // Serve React app for all other routes (excluding static files)
 app.get('*', (req, res) => {
     // Skip requests for static assets (files with extensions)
diff --git a/src/utils/api.js b/src/utils/api.js
index 0ac8d42..014aa2b 100644
--- a/src/utils/api.js
+++ b/src/utils/api.js
@@ -22,6 +22,18 @@ export const authenticatedFetch = (url, options = {}) => {
       ...options.headers,
     },
   }).then((response) => {
+    // Detect when a reverse-proxy (e.g. oauth2-proxy) returns an HTML sign-in
+    // page instead of the expected JSON.  This typically happens when the
+    // session cookie expires.  Force a full page reload so the browser goes
+    // through the auth flow again instead of showing a cryptic JSON-parse error.
+    const ct = response.headers.get('content-type') || '';
+    if (ct.includes('text/html') && url.startsWith('/api/')) {
+      console.warn('[auth] API response returned HTML — session likely expired, reloading');
+      window.location.reload();
+      // Return a never-resolving promise so callers don't try to parse the HTML
+      return new Promise(() => {});
+    }
+
     const refreshedToken = response.headers.get('X-Refreshed-Token');
     if (refreshedToken) {
       localStorage.setItem('auth-token', refreshedToken);
