diff --git a/server/modules/database/migrations.ts b/server/modules/database/migrations.ts
index 60debe5..09de8eb 100644
--- a/server/modules/database/migrations.ts
+++ b/server/modules/database/migrations.ts
@@ -405,6 +405,14 @@ export const runMigrations = (db: Database) => {
       'BOOLEAN DEFAULT 0'
     );
 
+    // user_credentials: add git_name / git_email so a token can carry its own
+    // commit identity (used when cloning a new repo via that token).
+    if (tableExists(db, 'user_credentials')) {
+      const credColumnNames = getTableInfo(db, 'user_credentials').map((c) => c.name);
+      addColumnToTableIfNotExists(db, 'user_credentials', credColumnNames, 'git_name', 'TEXT');
+      addColumnToTableIfNotExists(db, 'user_credentials', credColumnNames, 'git_email', 'TEXT');
+    }
+
     db.exec(APP_CONFIG_TABLE_SCHEMA_SQL);
     db.exec(USER_NOTIFICATION_PREFERENCES_TABLE_SCHEMA_SQL);
     db.exec(VAPID_KEYS_TABLE_SCHEMA_SQL);
diff --git a/server/modules/database/repositories/credentials.ts b/server/modules/database/repositories/credentials.ts
index 8bb1110..522bf14 100644
--- a/server/modules/database/repositories/credentials.ts
+++ b/server/modules/database/repositories/credentials.ts
@@ -17,24 +17,28 @@ import type {
 // ---------------------------------------------------------------------------
 
 export const credentialsDb = {
-  /** Stores a new credential and returns a safe (no raw value) result. */
+  /** Stores a new credential (with optional per-token git identity) and returns a safe (no raw value) result. */
   createCredential(
     userId: number,
     credentialName: string,
     credentialType: string,
     credentialValue: string,
-    description: string | null = null
+    description: string | null = null,
+    gitName: string | null = null,
+    gitEmail: string | null = null
   ): CreateCredentialResult {
     const db = getConnection();
     const result = db
       .prepare(
-        'INSERT INTO user_credentials (user_id, credential_name, credential_type, credential_value, description) VALUES (?, ?, ?, ?, ?)'
+        'INSERT INTO user_credentials (user_id, credential_name, credential_type, credential_value, description, git_name, git_email) VALUES (?, ?, ?, ?, ?, ?, ?)'
       )
-      .run(userId, credentialName, credentialType, credentialValue, description);
+      .run(userId, credentialName, credentialType, credentialValue, description, gitName, gitEmail);
     return {
       id: result.lastInsertRowid,
       credentialName,
       credentialType,
+      gitName,
+      gitEmail,
     };
   },
 
@@ -51,14 +55,14 @@ export const credentialsDb = {
     if (credentialType) {
       return db
         .prepare(
-          'SELECT id, credential_name, credential_type, description, created_at, is_active FROM user_credentials WHERE user_id = ? AND credential_type = ? ORDER BY created_at DESC'
+          'SELECT id, credential_name, credential_type, description, git_name, git_email, created_at, is_active FROM user_credentials WHERE user_id = ? AND credential_type = ? ORDER BY created_at DESC'
         )
         .all(userId, credentialType) as CredentialPublicRow[];
     }
 
     return db
       .prepare(
-        'SELECT id, credential_name, credential_type, description, created_at, is_active FROM user_credentials WHERE user_id = ? ORDER BY created_at DESC'
+        'SELECT id, credential_name, credential_type, description, git_name, git_email, created_at, is_active FROM user_credentials WHERE user_id = ? ORDER BY created_at DESC'
       )
       .all(userId) as CredentialPublicRow[];
   },
@@ -80,6 +84,36 @@ export const credentialsDb = {
     return row?.credential_value ?? null;
   },
 
+  /** Update per-token git identity fields (leave token value untouched). */
+  updateCredentialIdentity(
+    userId: number,
+    credentialId: number,
+    gitName: string | null,
+    gitEmail: string | null
+  ): boolean {
+    const db = getConnection();
+    const result = db
+      .prepare(
+        'UPDATE user_credentials SET git_name = ?, git_email = ? WHERE id = ? AND user_id = ?'
+      )
+      .run(gitName, gitEmail, credentialId, userId);
+    return result.changes > 0;
+  },
+
+  /** Returns a single credential by id (full row including value -- used by post-clone handler). */
+  getCredentialById(
+    userId: number,
+    credentialId: number
+  ): (CredentialPublicRow & { credential_value: string }) | null {
+    const db = getConnection();
+    const row = db
+      .prepare(
+        'SELECT id, credential_name, credential_type, credential_value, description, git_name, git_email, created_at, is_active FROM user_credentials WHERE id = ? AND user_id = ?'
+      )
+      .get(credentialId, userId) as (CredentialPublicRow & { credential_value: string }) | undefined;
+    return row ?? null;
+  },
+
   /** Permanently removes a credential. Returns true if a row was deleted. */
   deleteCredential(userId: number, credentialId: number): boolean {
     const db = getConnection();
diff --git a/server/modules/database/repositories/github-tokens.ts b/server/modules/database/repositories/github-tokens.ts
index 475f515..cec6040 100644
--- a/server/modules/database/repositories/github-tokens.ts
+++ b/server/modules/database/repositories/github-tokens.ts
@@ -21,6 +21,8 @@ type CredentialRow = {
   credential_type: string;
   credential_value: string;
   description: string | null;
+  git_name: string | null;
+  git_email: string | null;
   created_at: string;
   is_active: number;
 };
@@ -30,19 +32,23 @@ type GithubTokenLookup = CredentialRow & {
 };
 
 export const githubTokensDb = {
-  /** Creates a GitHub token credential entry. */
+  /** Creates a GitHub token credential entry (with optional per-token git identity). */
   createGithubToken(
     userId: number,
     tokenName: string,
     githubToken: string,
-    description: string | null = null
+    description: string | null = null,
+    gitName: string | null = null,
+    gitEmail: string | null = null
   ): CreateCredentialResult {
     return credentialsDb.createCredential(
       userId,
       tokenName,
       GITHUB_TOKEN_TYPE,
       githubToken,
-      description
+      description,
+      gitName,
+      gitEmail
     );
   },
 
diff --git a/server/modules/projects/services/project-clone.service.ts b/server/modules/projects/services/project-clone.service.ts
index 1a91b87..658c150 100644
--- a/server/modules/projects/services/project-clone.service.ts
+++ b/server/modules/projects/services/project-clone.service.ts
@@ -2,7 +2,7 @@ import { spawn } from 'node:child_process';
 import { access, mkdir, rm } from 'node:fs/promises';
 import path from 'node:path';
 
-import { githubTokensDb } from '@/modules/database/index.js';
+import { credentialsDb, githubTokensDb } from '@/modules/database/index.js';
 import { createProject } from '@/modules/projects/services/project-management.service.js';
 import type { WorkspacePathValidationResult } from '@/shared/types.js';
 import { AppError, validateWorkspacePath } from '@/shared/utils.js';
@@ -33,6 +33,12 @@ type GitCloneProcess = {
   kill(): void;
 };
 
+type TokenIdentity = {
+  name: string | null;
+  gitName: string | null;
+  gitEmail: string | null;
+} | null;
+
 type CloneProjectDependencies = {
   validatePath: (requestedPath: string) => Promise<WorkspacePathValidationResult>;
   ensureDirectory: (directoryPath: string) => Promise<void>;
@@ -42,8 +48,13 @@ type CloneProjectDependencies = {
     tokenId: number,
     userId: number,
   ) => Promise<{ github_token: string } | null>;
+  getTokenIdentity: (
+    tokenId: number,
+    userId: number,
+  ) => Promise<TokenIdentity>;
   spawnGitClone: (cloneUrl: string, clonePath: string) => GitCloneProcess;
   registerProject: (projectPath: string, customName: string) => Promise<{ project: Record<string, unknown> }>;
+  applyPostCloneSettings: (repoPath: string, originalGithubUrl: string, tokenIdentity: TokenIdentity) => Promise<void>;
   logError: (message: string, error: unknown) => void;
 };
 
@@ -140,11 +151,81 @@ const defaultDependencies: CloneProjectDependencies = {
       projectPath,
       customName,
     }) as Promise<{ project: Record<string, unknown> }>,
+  getTokenIdentity: async (
+    tokenId: number,
+    userId: number,
+  ): Promise<TokenIdentity> => {
+    const row = credentialsDb.getCredentialById(userId, tokenId);
+    if (!row) return null;
+    return {
+      name: row.credential_name || null,
+      gitName: (row as any).git_name || null,
+      gitEmail: (row as any).git_email || null,
+    };
+  },
+  applyPostCloneSettings: applyPostCloneSettingsImpl,
   logError: (message: string, error: unknown): void => {
     console.error(message, error);
   },
 };
 
+/**
+ * After a successful clone:
+ *   1) Reset the remote URL to the plain form (strip any embedded PAT).
+ *   2) If the credential has a per-token git_name / git_email, write them as
+ *      repo-local user.name / user.email.
+ *   3) If the credential has a name, write `credential.<remote>.username=<name>` so
+ *      future pull/push on this repo are routed by our git credential helper to the
+ *      specific token in the DB.
+ */
+function applyPostCloneSettingsImpl(
+  repoPath: string,
+  originalGithubUrl: string,
+  tokenIdentity: TokenIdentity
+): Promise<void> {
+  return new Promise((resolve) => {
+    const isSshUrl = /^(ssh:\/\/|[\w.-]+@[\w.-]+:)/.test(originalGithubUrl);
+
+    const cleanUrl = (() => {
+      if (isSshUrl) return originalGithubUrl;
+      try {
+        const u = new URL(originalGithubUrl);
+        u.username = '';
+        u.password = '';
+        return u.toString().replace(/\/$/, '');
+      } catch {
+        return originalGithubUrl;
+      }
+    })();
+
+    const cmds: [string, string[]][] = [
+      ['git', ['-C', repoPath, 'remote', 'set-url', 'origin', cleanUrl]],
+    ];
+
+    if (tokenIdentity?.gitName) {
+      cmds.push(['git', ['-C', repoPath, 'config', '--local', 'user.name', tokenIdentity.gitName]]);
+    }
+    if (tokenIdentity?.gitEmail) {
+      cmds.push(['git', ['-C', repoPath, 'config', '--local', 'user.email', tokenIdentity.gitEmail]]);
+    }
+    // Only for https URLs: wire credential.<url>.username so our helper picks the right token
+    if (!isSshUrl && tokenIdentity?.name) {
+      const credKey = `credential.${cleanUrl.replace(/\.git$/, '')}.username`;
+      cmds.push(['git', ['-C', repoPath, 'config', '--local', credKey, tokenIdentity.name]]);
+    }
+
+    let i = 0;
+    const runNext = () => {
+      if (i >= cmds.length) return resolve();
+      const [bin, args] = cmds[i++];
+      const p = spawn(bin, args, { stdio: 'ignore' });
+      p.on('close', runNext);
+      p.on('error', runNext); // best-effort; don't fail the clone on post-config issues
+    };
+    runNext();
+  });
+}
+
 export async function startCloneProject(
   input: CloneProjectInput,
   handlers: CloneProjectEventHandlers,
@@ -258,6 +339,19 @@ export async function startCloneProject(
     gitProcess.on('close', async (code) => {
       if (code === 0) {
         try {
+          // Apply per-token identity and credential routing
+          let tokenIdentity: TokenIdentity = null;
+          if (typeof input.githubTokenId === 'number') {
+            const numericUserId =
+              typeof input.userId === 'number'
+                ? input.userId
+                : Number.parseInt(String(input.userId), 10);
+            if (!Number.isNaN(numericUserId)) {
+              tokenIdentity = await dependencies.getTokenIdentity(input.githubTokenId, numericUserId);
+            }
+          }
+          await dependencies.applyPostCloneSettings(clonePath, normalizedGithubUrl, tokenIdentity);
+
           const createdProject = await dependencies.registerProject(clonePath, repoName);
           handlers.onComplete({
             project: createdProject.project,
diff --git a/server/routes/settings.js b/server/routes/settings.js
index d467c49..275ace9 100644
--- a/server/routes/settings.js
+++ b/server/routes/settings.js
@@ -102,10 +102,10 @@ router.get('/credentials', async (req, res) => {
   }
 });
 
-// Create a new credential
+// Create a new credential (optionally carries a per-token git identity)
 router.post('/credentials', async (req, res) => {
   try {
-    const { credentialName, credentialType, credentialValue, description } = req.body;
+    const { credentialName, credentialType, credentialValue, description, gitName, gitEmail } = req.body;
 
     if (!credentialName || !credentialName.trim()) {
       return res.status(400).json({ error: 'Credential name is required' });
@@ -124,7 +124,9 @@ router.post('/credentials', async (req, res) => {
       credentialName.trim(),
       credentialType.trim(),
       credentialValue.trim(),
-      description?.trim() || null
+      description?.trim() || null,
+      gitName?.trim() || null,
+      gitEmail?.trim() || null
     );
 
     res.json({
@@ -137,6 +139,28 @@ router.post('/credentials', async (req, res) => {
   }
 });
 
+// Update per-credential git identity (optional fields, null to clear)
+router.patch('/credentials/:credentialId/identity', async (req, res) => {
+  try {
+    const { credentialId } = req.params;
+    const { gitName, gitEmail } = req.body;
+    const success = credentialsDb.updateCredentialIdentity(
+      req.user.id,
+      parseInt(credentialId),
+      gitName?.trim() || null,
+      gitEmail?.trim() || null
+    );
+    if (success) {
+      res.json({ success: true });
+    } else {
+      res.status(404).json({ error: 'Credential not found' });
+    }
+  } catch (error) {
+    console.error('Error updating credential identity:', error);
+    res.status(500).json({ error: 'Failed to update credential identity' });
+  }
+});
+
 // Delete a credential
 router.delete('/credentials/:credentialId', async (req, res) => {
   try {
diff --git a/server/shared/types.ts b/server/shared/types.ts
index d15f69e..0b67dde 100644
--- a/server/shared/types.ts
+++ b/server/shared/types.ts
@@ -259,6 +259,8 @@ export type CredentialPublicRow = {
   credential_name: string;
   credential_type: string;
   description: string | null;
+  git_name: string | null;
+  git_email: string | null;
   created_at: string;
   is_active: number;
 };
@@ -273,6 +275,8 @@ export type CreateCredentialResult = {
   id: number | bigint;
   credentialName: string;
   credentialType: string;
+  gitName?: string | null;
+  gitEmail?: string | null;
 };
 
 // ---------------------------
diff --git a/src/components/settings/hooks/useCredentialsSettings.ts b/src/components/settings/hooks/useCredentialsSettings.ts
index 4a9afc4..79bf8d2 100644
--- a/src/components/settings/hooks/useCredentialsSettings.ts
+++ b/src/components/settings/hooks/useCredentialsSettings.ts
@@ -33,6 +33,8 @@ export function useCredentialsSettings({
   const [newGithubName, setNewGithubName] = useState('');
   const [newGithubToken, setNewGithubToken] = useState('');
   const [newGithubDescription, setNewGithubDescription] = useState('');
+  const [newGithubGitName, setNewGithubGitName] = useState('');
+  const [newGithubGitEmail, setNewGithubGitEmail] = useState('');
 
   const [showToken, setShowToken] = useState<Record<string, boolean>>({});
   const [copiedKey, setCopiedKey] = useState<string | null>(null);
@@ -143,6 +145,8 @@ export function useCredentialsSettings({
           credentialType: 'github_token',
           credentialValue: newGithubToken,
           description: newGithubDescription.trim(),
+          gitName: newGithubGitName.trim() || null,
+          gitEmail: newGithubGitEmail.trim() || null,
         }),
       });
 
@@ -155,13 +159,15 @@ export function useCredentialsSettings({
       setNewGithubName('');
       setNewGithubToken('');
       setNewGithubDescription('');
+      setNewGithubGitName('');
+      setNewGithubGitEmail('');
       setShowNewGithubForm(false);
       setShowToken((prev) => ({ ...prev, new: false }));
       await fetchData();
     } catch (error) {
       console.error('Error creating GitHub credential:', error);
     }
-  }, [fetchData, newGithubDescription, newGithubName, newGithubToken]);
+  }, [fetchData, newGithubDescription, newGithubName, newGithubToken, newGithubGitName, newGithubGitEmail]);
 
   const deleteGithubCredential = useCallback(async (credentialId: string) => {
     if (!window.confirm(confirmDeleteGithubCredentialText)) {
@@ -228,6 +234,8 @@ export function useCredentialsSettings({
     setNewGithubName('');
     setNewGithubToken('');
     setNewGithubDescription('');
+    setNewGithubGitName('');
+    setNewGithubGitEmail('');
     setShowToken((prev) => ({ ...prev, new: false }));
   }, []);
 
@@ -255,6 +263,10 @@ export function useCredentialsSettings({
     setNewGithubToken,
     newGithubDescription,
     setNewGithubDescription,
+    newGithubGitName,
+    setNewGithubGitName,
+    newGithubGitEmail,
+    setNewGithubGitEmail,
     showToken,
     copiedKey,
     newlyCreatedKey,
diff --git a/src/components/settings/view/tabs/api-settings/CredentialsSettingsTab.tsx b/src/components/settings/view/tabs/api-settings/CredentialsSettingsTab.tsx
index 89d440c..340d0f7 100644
--- a/src/components/settings/view/tabs/api-settings/CredentialsSettingsTab.tsx
+++ b/src/components/settings/view/tabs/api-settings/CredentialsSettingsTab.tsx
@@ -22,6 +22,10 @@ export default function CredentialsSettingsTab() {
     setNewGithubToken,
     newGithubDescription,
     setNewGithubDescription,
+    newGithubGitName,
+    setNewGithubGitName,
+    newGithubGitEmail,
+    setNewGithubGitEmail,
     showToken,
     copiedKey,
     newlyCreatedKey,
@@ -75,10 +79,14 @@ export default function CredentialsSettingsTab() {
         newGithubName={newGithubName}
         newGithubToken={newGithubToken}
         newGithubDescription={newGithubDescription}
+        newGithubGitName={newGithubGitName}
+        newGithubGitEmail={newGithubGitEmail}
         onShowNewGithubFormChange={setShowNewGithubForm}
         onNewGithubNameChange={setNewGithubName}
         onNewGithubTokenChange={setNewGithubToken}
         onNewGithubDescriptionChange={setNewGithubDescription}
+        onNewGithubGitNameChange={setNewGithubGitName}
+        onNewGithubGitEmailChange={setNewGithubGitEmail}
         onToggleNewTokenVisibility={toggleNewGithubTokenVisibility}
         onCreateGithubCredential={createGithubCredential}
         onCancelCreateGithubCredential={cancelNewGithubForm}
diff --git a/src/components/settings/view/tabs/api-settings/sections/GithubCredentialsSection.tsx b/src/components/settings/view/tabs/api-settings/sections/GithubCredentialsSection.tsx
index 683fcf5..38a18eb 100644
--- a/src/components/settings/view/tabs/api-settings/sections/GithubCredentialsSection.tsx
+++ b/src/components/settings/view/tabs/api-settings/sections/GithubCredentialsSection.tsx
@@ -10,10 +10,14 @@ type GithubCredentialsSectionProps = {
   newGithubName: string;
   newGithubToken: string;
   newGithubDescription: string;
+  newGithubGitName: string;
+  newGithubGitEmail: string;
   onShowNewGithubFormChange: (value: boolean) => void;
   onNewGithubNameChange: (value: string) => void;
   onNewGithubTokenChange: (value: string) => void;
   onNewGithubDescriptionChange: (value: string) => void;
+  onNewGithubGitNameChange: (value: string) => void;
+  onNewGithubGitEmailChange: (value: string) => void;
   onToggleNewTokenVisibility: () => void;
   onCreateGithubCredential: () => void;
   onCancelCreateGithubCredential: () => void;
@@ -28,10 +32,14 @@ export default function GithubCredentialsSection({
   newGithubName,
   newGithubToken,
   newGithubDescription,
+  newGithubGitName,
+  newGithubGitEmail,
   onShowNewGithubFormChange,
   onNewGithubNameChange,
   onNewGithubTokenChange,
   onNewGithubDescriptionChange,
+  onNewGithubGitNameChange,
+  onNewGithubGitEmailChange,
   onToggleNewTokenVisibility,
   onCreateGithubCredential,
   onCancelCreateGithubCredential,
@@ -87,6 +95,27 @@ export default function GithubCredentialsSection({
             onChange={(event) => onNewGithubDescriptionChange(event.target.value)}
           />
 
+          <div className="rounded-md border border-dashed bg-muted/30 p-3">
+            <p className="mb-2 text-xs font-medium text-muted-foreground">
+              Per-token git identity (optional)
+            </p>
+            <p className="mb-3 text-xs text-muted-foreground">
+              If set, new repos cloned with this token get these as their commit author. Leave empty to inherit the global Settings &rarr; Git identity.
+            </p>
+            <div className="space-y-2">
+              <Input
+                placeholder='Git name (e.g. "Xyster3k")'
+                value={newGithubGitName}
+                onChange={(event) => onNewGithubGitNameChange(event.target.value)}
+              />
+              <Input
+                placeholder='Git email (e.g. "141751440+xyster3k@users.noreply.github.com")'
+                value={newGithubGitEmail}
+                onChange={(event) => onNewGithubGitEmailChange(event.target.value)}
+              />
+            </div>
+          </div>
+
           <div className="flex gap-2">
             <Button onClick={onCreateGithubCredential}>{t('apiKeys.github.form.addButton')}</Button>
             <Button variant="outline" onClick={onCancelCreateGithubCredential}>
@@ -116,6 +145,11 @@ export default function GithubCredentialsSection({
                 {credential.description && (
                   <div className="text-xs text-muted-foreground">{credential.description}</div>
                 )}
+                {(credential.git_name || credential.git_email) && (
+                  <div className="mt-1 text-xs text-muted-foreground">
+                    identity: {credential.git_name || '(inherit)'} &lt;{credential.git_email || '(inherit)'}&gt;
+                  </div>
+                )}
                 <div className="mt-1 text-xs text-muted-foreground">
                   {t('apiKeys.github.added')} {new Date(credential.created_at).toLocaleDateString()}
                 </div>
diff --git a/src/components/settings/view/tabs/api-settings/types.ts b/src/components/settings/view/tabs/api-settings/types.ts
index b3d3ae1..1990d48 100644
--- a/src/components/settings/view/tabs/api-settings/types.ts
+++ b/src/components/settings/view/tabs/api-settings/types.ts
@@ -18,6 +18,8 @@ export type GithubCredentialItem = {
   id: string;
   credential_name: string;
   description?: string | null;
+  git_name?: string | null;
+  git_email?: string | null;
   created_at: string;
   is_active: boolean;
 };
