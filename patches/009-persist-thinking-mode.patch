diff --git a/src/components/chat/hooks/useChatComposerState.ts b/src/components/chat/hooks/useChatComposerState.ts
index 3b16721..40d184f 100644
--- a/src/components/chat/hooks/useChatComposerState.ts
+++ b/src/components/chat/hooks/useChatComposerState.ts
@@ -143,7 +143,18 @@ export function useChatComposerState({
   const [uploadingImages, setUploadingImages] = useState<Map<string, number>>(new Map());
   const [imageErrors, setImageErrors] = useState<Map<string, string>>(new Map());
   const [isTextareaExpanded, setIsTextareaExpanded] = useState(false);
-  const [thinkingMode, setThinkingMode] = useState('none');
+  const [thinkingMode, _setThinkingMode] = useState(() => {
+    if (typeof window !== 'undefined') {
+      return localStorage.getItem('cloudcli_thinking_mode') || 'none';
+    }
+    return 'none';
+  });
+  const setThinkingMode = useCallback((mode: string) => {
+    _setThinkingMode(mode);
+    if (typeof window !== 'undefined') {
+      localStorage.setItem('cloudcli_thinking_mode', mode);
+    }
+  }, []);
 
   const textareaRef = useRef<HTMLTextAreaElement>(null);
   const inputHighlightRef = useRef<HTMLDivElement>(null);
@@ -663,7 +674,6 @@ export function useChatComposerState({
       setUploadingImages(new Map());
       setImageErrors(new Map());
       setIsTextareaExpanded(false);
-      setThinkingMode('none');
 
       if (textareaRef.current) {
         textareaRef.current.style.height = 'auto';
