diff --git a/server/index.js b/server/index.js
index d8223e2..b1cfc61 100755
--- a/server/index.js
+++ b/server/index.js
@@ -45,6 +45,7 @@ import {
     isGeminiSessionActive,
     getActiveGeminiSessions,
 } from './gemini-cli.js';
+import { resolveBuilderApproval, runBuilderWorkflow } from './builder-mode.js';
 import sessionManager from './sessionManager.js';
 import {
     stripAnsiSequences,
@@ -109,6 +110,8 @@ const wss = createWebSocketServer(server, {
         getActiveCursorSessions,
         getActiveCodexSessions,
         getActiveGeminiSessions,
+        runBuilderWorkflow,
+        resolveBuilderApproval,
     },
     shell: {
         getSessionById: (sessionId) => sessionManager.getSession(sessionId),
diff --git a/server/modules/websocket/services/chat-websocket.service.ts b/server/modules/websocket/services/chat-websocket.service.ts
index 5c8d5b8..c1e2aa6 100644
--- a/server/modules/websocket/services/chat-websocket.service.ts
+++ b/server/modules/websocket/services/chat-websocket.service.ts
@@ -52,6 +52,8 @@ type ChatWebSocketDependencies = {
   getActiveCursorSessions: () => unknown;
   getActiveCodexSessions: () => unknown;
   getActiveGeminiSessions: () => unknown;
+  runBuilderWorkflow: (command: string, options: unknown, writer: WebSocketWriter, provider: string) => Promise<unknown>;
+  resolveBuilderApproval: (requestId: string, decision: { allow: boolean; message?: string }) => boolean;
 };
 
 /**
@@ -114,6 +116,20 @@ export function handleChatConnection(
         throw new Error('Message type is required');
       }
 
+      const workflowMode = (data.options as Record<string, unknown>)?.workflowMode as string | undefined;
+      if (
+        workflowMode === 'builder'
+        && data.command
+        && (messageType === 'claude-command' || messageType === 'codex-command' || messageType === 'cursor-command' || messageType === 'gemini-command')
+      ) {
+        const provider = messageType === 'cursor-command' ? 'cursor'
+          : messageType === 'codex-command' ? 'codex'
+          : messageType === 'gemini-command' ? 'gemini'
+          : 'claude';
+        await dependencies.runBuilderWorkflow(data.command ?? '', data.options ?? {}, writer, provider);
+        return;
+      }
+
       if (messageType === 'claude-command') {
         const sessionId = (data.options as Record<string, unknown>)?.sessionId as string | undefined;
         if (sessionId && dependencies.isClaudeSDKSessionActive(sessionId)) {
@@ -193,6 +209,13 @@ export function handleChatConnection(
         return;
       }
 
+      if (messageType === 'builder-approval-response') {
+        const requestId = (data as Record<string, unknown>).requestId as string;
+        const decision = (data as Record<string, unknown>).decision as { allow: boolean; message?: string };
+        if (requestId) dependencies.resolveBuilderApproval(requestId, decision || { allow: false });
+        return;
+      }
+
       if (messageType === 'cursor-abort') {
         const sessionId = typeof data.sessionId === 'string' ? data.sessionId : '';
         const success = dependencies.abortCursorSession(sessionId);
diff --git a/src/components/chat/hooks/useChatComposerState.ts b/src/components/chat/hooks/useChatComposerState.ts
index 08bfb1a..9b64d57 100644
--- a/src/components/chat/hooks/useChatComposerState.ts
+++ b/src/components/chat/hooks/useChatComposerState.ts
@@ -18,6 +18,7 @@ import type {
   ChatMessage,
   PendingPermissionRequest,
   PermissionMode,
+  WorkflowMode,
 } from '../types/types';
 import type { Project, ProjectSession, LLMProvider } from '../../../types/app';
 import { escapeRegExp } from '../utils/chatFormatting';
@@ -59,6 +60,7 @@ interface UseChatComposerStateArgs {
   setCanAbortSession: (canAbort: boolean) => void;
   setClaudeStatus: (status: { text: string; tokens: number; can_interrupt: boolean } | null) => void;
   setIsUserScrolledUp: (isScrolledUp: boolean) => void;
+  pendingPermissionRequests: PendingPermissionRequest[];
   setPendingPermissionRequests: Dispatch<SetStateAction<PendingPermissionRequest[]>>;
 }
 
@@ -131,6 +133,7 @@ export function useChatComposerState({
   setCanAbortSession,
   setClaudeStatus,
   setIsUserScrolledUp,
+  pendingPermissionRequests,
   setPendingPermissionRequests,
 }: UseChatComposerStateArgs) {
   const [input, setInput] = useState(() => {
@@ -157,6 +160,13 @@ export function useChatComposerState({
       localStorage.setItem('cloudcli_thinking_mode', mode);
     }
   }, []);
+  const [workflowMode, setWorkflowMode] = useState<WorkflowMode>(() => {
+    if (typeof window === 'undefined' || !selectedProject) {
+      return 'chat';
+    }
+    const savedMode = safeLocalStorage.getItem(`workflow_mode_${selectedProject.projectId}`);
+    return savedMode === 'builder' ? 'builder' : 'chat';
+  });
 
   const textareaRef = useRef<HTMLTextAreaElement>(null);
   const inputHighlightRef = useRef<HTMLDivElement>(null);
@@ -620,6 +630,7 @@ export function useChatComposerState({
             skipPermissions: toolsSettings?.skipPermissions || false,
             sessionSummary,
             toolsSettings,
+            workflowMode,
           },
         });
       } else if (provider === 'codex') {
@@ -635,6 +646,7 @@ export function useChatComposerState({
             model: codexModel,
             sessionSummary,
             permissionMode: permissionMode === 'plan' ? 'default' : permissionMode,
+            workflowMode,
           },
         });
       } else if (provider === 'gemini') {
@@ -651,6 +663,7 @@ export function useChatComposerState({
             sessionSummary,
             permissionMode,
             toolsSettings,
+            workflowMode,
           },
         });
       } else {
@@ -667,6 +680,7 @@ export function useChatComposerState({
             model: claudeModel,
             sessionSummary,
             images: uploadedImages,
+            workflowMode,
           },
         });
       }
@@ -711,6 +725,7 @@ export function useChatComposerState({
       setIsUserScrolledUp,
       slashCommands,
       thinkingMode,
+      workflowMode,
     ],
   );
 
@@ -722,6 +737,21 @@ export function useChatComposerState({
     inputValueRef.current = input;
   }, [input]);
 
+  useEffect(() => {
+    if (!selectedProject) {
+      return;
+    }
+    safeLocalStorage.setItem(`workflow_mode_${selectedProject.projectId}`, workflowMode);
+  }, [workflowMode, selectedProject]);
+
+  useEffect(() => {
+    if (!selectedProject) {
+      return;
+    }
+    const savedMode = safeLocalStorage.getItem(`workflow_mode_${selectedProject.projectId}`);
+    setWorkflowMode(savedMode === 'builder' ? 'builder' : 'chat');
+  }, [selectedProject?.projectId]);
+
   useEffect(() => {
     if (!selectedProject) {
       return;
@@ -888,6 +918,10 @@ export function useChatComposerState({
     });
   }, [currentSessionId, pendingViewSessionRef, provider, selectedSession?.id, sendMessage]);
 
+  const toggleWorkflowMode = useCallback(() => {
+    setWorkflowMode((current: WorkflowMode) => (current === 'chat' ? 'builder' : 'chat'));
+  }, []);
+
   const handleGrantToolPermission = useCallback(
     (suggestion: { entry: string; toolName: string }) => {
       if (!suggestion || provider !== 'claude') {
@@ -910,8 +944,9 @@ export function useChatComposerState({
       }
 
       validIds.forEach((requestId) => {
+        const request = pendingPermissionRequests.find((item) => item.requestId === requestId);
         sendMessage({
-          type: 'claude-permission-response',
+          type: request?.source === 'builder' ? 'builder-approval-response' : 'claude-permission-response',
           requestId,
           allow: Boolean(decision?.allow),
           updatedInput: decision?.updatedInput,
@@ -928,7 +963,7 @@ export function useChatComposerState({
         return next;
       });
     },
-    [sendMessage, setClaudeStatus, setPendingPermissionRequests],
+    [pendingPermissionRequests, sendMessage, setClaudeStatus, setPendingPermissionRequests],
   );
 
   const [isInputFocused, setIsInputFocused] = useState(false);
@@ -980,6 +1015,8 @@ export function useChatComposerState({
     syncInputOverlayScroll,
     handleClearInput,
     handleAbortSession,
+    workflowMode,
+    toggleWorkflowMode,
     handlePermissionDecision,
     handleGrantToolPermission,
     handleInputFocusChange,
diff --git a/src/components/chat/hooks/useChatRealtimeHandlers.ts b/src/components/chat/hooks/useChatRealtimeHandlers.ts
index 855ee78..f2b9b91 100644
--- a/src/components/chat/hooks/useChatRealtimeHandlers.ts
+++ b/src/components/chat/hooks/useChatRealtimeHandlers.ts
@@ -303,6 +303,7 @@ export function useChatRealtimeHandlers({
           return [...prev, {
             requestId: msg.requestId,
             toolName: msg.toolName || 'UnknownTool',
+            source: msg.source === 'builder' ? 'builder' : 'claude',
             input: msg.input,
             context: msg.context,
             sessionId: sid || null,
diff --git a/src/components/chat/types/types.ts b/src/components/chat/types/types.ts
index 526b8cc..f38e846 100644
--- a/src/components/chat/types/types.ts
+++ b/src/components/chat/types/types.ts
@@ -3,6 +3,7 @@ import type { Project, ProjectSession, LLMProvider } from '../../../types/app';
 export type Provider = LLMProvider;
 
 export type PermissionMode = 'default' | 'acceptEdits' | 'auto' | 'bypassPermissions' | 'plan';
+export type WorkflowMode = 'chat' | 'builder';
 
 export interface ChatImage {
   data: string;
@@ -73,6 +74,7 @@ export interface PermissionGrantResult {
 export interface PendingPermissionRequest {
   requestId: string;
   toolName: string;
+  source?: 'claude' | 'builder';
   input?: unknown;
   context?: unknown;
   sessionId?: string | null;
diff --git a/src/components/chat/view/ChatInterface.tsx b/src/components/chat/view/ChatInterface.tsx
index 5c31a60..b546b60 100644
--- a/src/components/chat/view/ChatInterface.tsx
+++ b/src/components/chat/view/ChatInterface.tsx
@@ -168,6 +168,8 @@ function ChatInterface({
     syncInputOverlayScroll,
     handleClearInput,
     handleAbortSession,
+    workflowMode,
+    toggleWorkflowMode,
     handlePermissionDecision,
     handleGrantToolPermission,
     handleInputFocusChange,
@@ -202,6 +204,7 @@ function ChatInterface({
     setCanAbortSession,
     setClaudeStatus,
     setIsUserScrolledUp,
+    pendingPermissionRequests,
     setPendingPermissionRequests,
   });
 
@@ -355,6 +358,8 @@ function ChatInterface({
           claudeStatus={claudeStatus}
           isLoading={isLoading}
           onAbortSession={handleAbortSession}
+          workflowMode={workflowMode}
+          onWorkflowModeToggle={toggleWorkflowMode}
           provider={provider}
           permissionMode={permissionMode}
           onModeSwitch={cyclePermissionMode}
@@ -402,7 +407,7 @@ function ChatInterface({
           onTextareaScrollSync={syncInputOverlayScroll}
           onTextareaInput={handleTextareaInput}
           onInputFocusChange={handleInputFocusChange}
-          placeholder={t('input.placeholder', {
+          placeholder={t(workflowMode === 'builder' ? 'input.builderPlaceholder' : 'input.placeholder', {
             provider:
               provider === 'cursor'
                 ? t('messageTypes.cursor')
diff --git a/src/components/chat/view/subcomponents/ChatComposer.tsx b/src/components/chat/view/subcomponents/ChatComposer.tsx
index 1c8ff2d..8131e77 100644
--- a/src/components/chat/view/subcomponents/ChatComposer.tsx
+++ b/src/components/chat/view/subcomponents/ChatComposer.tsx
@@ -11,8 +11,8 @@ import type {
   SetStateAction,
   TouchEvent,
 } from 'react';
-import { ImageIcon, MessageSquareIcon, XIcon, ArrowDownIcon } from 'lucide-react';
-import type { PendingPermissionRequest, PermissionMode, Provider } from '../../types/types';
+import { ImageIcon, MessageSquareIcon, WrenchIcon, XIcon, ArrowDownIcon } from 'lucide-react';
+import type { PendingPermissionRequest, PermissionMode, Provider, WorkflowMode } from '../../types/types';
 import CommandMenu from './CommandMenu';
 import ClaudeStatus from './ClaudeStatus';
 import ImageAttachment from './ImageAttachment';
@@ -55,6 +55,8 @@ interface ChatComposerProps {
   claudeStatus: { text: string; tokens: number; can_interrupt: boolean } | null;
   isLoading: boolean;
   onAbortSession: () => void;
+  workflowMode: WorkflowMode;
+  onWorkflowModeToggle: () => void;
   provider: Provider | string;
   permissionMode: PermissionMode | string;
   onModeSwitch: () => void;
@@ -110,6 +112,8 @@ export default function ChatComposer({
   claudeStatus,
   isLoading,
   onAbortSession,
+  workflowMode,
+  onWorkflowModeToggle,
   provider,
   permissionMode,
   onModeSwitch,
@@ -317,6 +321,24 @@ export default function ChatComposer({
               <ImageIcon />
             </PromptInputButton>
 
+            <button
+              type="button"
+              onClick={onWorkflowModeToggle}
+              className={`rounded-lg border p-2 text-xs font-medium transition-all duration-200 sm:px-2.5 sm:py-1 ${
+                workflowMode === 'builder'
+                  ? 'border-sky-300/60 bg-sky-50 text-sky-700 hover:bg-sky-100 dark:border-sky-600/40 dark:bg-sky-900/15 dark:text-sky-300 dark:hover:bg-sky-900/25'
+                  : 'border-border/60 bg-muted/50 text-muted-foreground hover:bg-muted'
+              }`}
+              title={t('input.workflowModeToggle')}
+            >
+              <div className="flex items-center gap-1.5">
+                <WrenchIcon className="h-3.5 w-3.5" />
+                <span className="hidden whitespace-nowrap sm:inline">
+                  {workflowMode === 'builder' ? t('input.workflowModes.builder') : t('input.workflowModes.chat')}
+                </span>
+              </div>
+            </button>
+
             <button
               type="button"
               onClick={onModeSwitch}
diff --git a/src/i18n/locales/en/chat.json b/src/i18n/locales/en/chat.json
index 6db4d75..de2eb7c 100644
--- a/src/i18n/locales/en/chat.json
+++ b/src/i18n/locales/en/chat.json
@@ -123,6 +123,7 @@
   },
   "input": {
     "placeholder": "Type / for commands, @ for files, or ask {{provider}} anything...",
+    "builderPlaceholder": "Describe the change you want built. Builder Mode will draft a PRD and task plan before execution.",
     "placeholderDefault": "Type your message...",
     "disabled": "Input disabled",
     "attachFiles": "Attach files",
@@ -134,6 +135,11 @@
       "enter": "Enter to send • Shift+Enter for new line • Tab to change modes • / for slash commands"
     },
     "clickToChangeMode": "Click to change permission mode (or press Tab in input)",
+    "workflowModeToggle": "Toggle between normal chat and builder workflow mode",
+    "workflowModes": {
+      "chat": "Chat",
+      "builder": "Build"
+    },
     "showAllCommands": "Show all commands",
     "clearInput": "Clear input",
     "scrollToBottom": "Scroll to bottom"
