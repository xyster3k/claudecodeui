diff --git a/src/components/app/AppContent.tsx b/src/components/app/AppContent.tsx
index 63c11df..b189e12 100644
--- a/src/components/app/AppContent.tsx
+++ b/src/components/app/AppContent.tsx
@@ -7,6 +7,7 @@ import { useWebSocket } from '../../contexts/WebSocketContext';
 import { useDeviceSettings } from '../../hooks/useDeviceSettings';
 import { useSessionProtection } from '../../hooks/useSessionProtection';
 import { useProjectsState } from '../../hooks/useProjectsState';
+import type { LLMProvider } from '../../types/app';
 
 export default function AppContent() {
   const navigate = useNavigate();
@@ -142,11 +143,23 @@ export default function AppContent() {
     return () => vv.removeEventListener('resize', update);
   }, []);
 
+  const handleAbortSidebarSession = (targetSessionId: string, provider: LLMProvider) => {
+    sendMessage({
+      type: 'abort-session',
+      sessionId: targetSessionId,
+      provider,
+    });
+  };
+
   return (
     <div className="fixed inset-0 flex bg-background" style={{ bottom: 'var(--keyboard-height, 0px)' }}>
       {!isMobile ? (
         <div className="h-full flex-shrink-0 border-r border-border/50">
-          <Sidebar {...sidebarSharedProps} />
+          <Sidebar
+            {...sidebarSharedProps}
+            processingSessions={processingSessions}
+            onAbortSession={handleAbortSidebarSession}
+          />
         </div>
       ) : (
         <div
@@ -172,7 +185,11 @@ export default function AppContent() {
             onClick={(event) => event.stopPropagation()}
             onTouchStart={(event) => event.stopPropagation()}
           >
-            <Sidebar {...sidebarSharedProps} />
+            <Sidebar
+              {...sidebarSharedProps}
+              processingSessions={processingSessions}
+              onAbortSession={handleAbortSidebarSession}
+            />
           </div>
         </div>
       )}
diff --git a/src/components/sidebar/types/types.ts b/src/components/sidebar/types/types.ts
index 9154717..c69c110 100644
--- a/src/components/sidebar/types/types.ts
+++ b/src/components/sidebar/types/types.ts
@@ -28,10 +28,12 @@ export type SidebarProps = {
   onProjectSelect: (project: Project) => void;
   onSessionSelect: (session: ProjectSession) => void;
   onNewSession: (project: Project) => void;
+  onAbortSession: (sessionId: string, provider: LLMProvider) => void;
   onSessionDelete?: (sessionId: string) => void;
   onProjectDelete?: (projectName: string) => void;
   isLoading: boolean;
   loadingProgress: LoadingProgress | null;
+  processingSessions: Set<string>;
   onRefresh: () => Promise<void> | void;
   onShowSettings: () => void;
   showSettings: boolean;
diff --git a/src/components/sidebar/view/Sidebar.tsx b/src/components/sidebar/view/Sidebar.tsx
index 0d6c906..727650b 100644
--- a/src/components/sidebar/view/Sidebar.tsx
+++ b/src/components/sidebar/view/Sidebar.tsx
@@ -25,10 +25,12 @@ function Sidebar({
   onProjectSelect,
   onSessionSelect,
   onNewSession,
+  onAbortSession,
   onSessionDelete,
   onProjectDelete,
   isLoading,
   loadingProgress,
+  processingSessions,
   onRefresh,
   onShowSettings,
   showSettings,
@@ -145,6 +147,7 @@ function Sidebar({
     loadingSessions,
     initialSessionsLoaded,
     currentTime,
+    processingSessions,
     editingSession,
     editingSessionName,
     deletingProjects,
@@ -163,6 +166,7 @@ function Sidebar({
     },
     onDeleteProject: requestProjectDelete,
     onSessionSelect: handleSessionClick,
+    onAbortSession,
     onDeleteSession: showDeleteSessionConfirmation,
     onLoadMoreSessions: (project) => {
       void loadMoreSessions(project);
diff --git a/src/components/sidebar/view/subcomponents/SidebarProjectItem.tsx b/src/components/sidebar/view/subcomponents/SidebarProjectItem.tsx
index 69f4d3d..806d269 100644
--- a/src/components/sidebar/view/subcomponents/SidebarProjectItem.tsx
+++ b/src/components/sidebar/view/subcomponents/SidebarProjectItem.tsx
@@ -1,4 +1,4 @@
-import { Check, ChevronDown, ChevronRight, Edit3, Folder, FolderOpen, Star, Trash2, X } from 'lucide-react';
+import { Check, ChevronDown, ChevronRight, Edit3, Folder, FolderOpen, Square, Star, Trash2, X } from 'lucide-react';
 import type { TFunction } from 'i18next';
 import { Button } from '../../../../shared/view/ui';
 import { cn } from '../../../../lib/utils';
@@ -21,6 +21,7 @@ type SidebarProjectItemProps = {
   initialSessionsLoaded: boolean;
   isLoadingSessions: boolean;
   currentTime: Date;
+  processingSessions: Set<string>;
   editingSession: string | null;
   editingSessionName: string;
   tasksEnabled: boolean;
@@ -34,6 +35,7 @@ type SidebarProjectItemProps = {
   onSaveProjectName: (projectName: string) => void;
   onDeleteProject: (project: Project) => void;
   onSessionSelect: (session: SessionWithProvider, projectName: string) => void;
+  onAbortSession: (sessionId: string, provider: LLMProvider) => void;
   onDeleteSession: (
     projectName: string,
     sessionId: string,
@@ -71,6 +73,7 @@ export default function SidebarProjectItem({
   initialSessionsLoaded,
   isLoadingSessions,
   currentTime,
+  processingSessions,
   editingSession,
   editingSessionName,
   tasksEnabled,
@@ -84,6 +87,7 @@ export default function SidebarProjectItem({
   onSaveProjectName,
   onDeleteProject,
   onSessionSelect,
+  onAbortSession,
   onDeleteSession,
   onLoadMoreSessions,
   onNewSession,
@@ -99,6 +103,8 @@ export default function SidebarProjectItem({
   const sessionCountDisplay = getSessionCountDisplay(sessions, hasMoreSessions);
   const sessionCountLabel = `${sessionCountDisplay} session${sessions.length === 1 ? '' : 's'}`;
   const taskStatus = getTaskIndicatorStatus(project, mcpServerStatus);
+  const processingProjectSessions = sessions.filter((session) => processingSessions.has(session.id));
+  const runControlTarget = processingProjectSessions[0] || null;
 
   const toggleProject = () => onToggleProject(project.name);
   const toggleStarProject = () => onToggleStarProject(project.name);
@@ -212,6 +218,18 @@ export default function SidebarProjectItem({
                   </>
                 ) : (
                   <>
+                    {runControlTarget && (
+                      <button
+                        className="flex h-8 w-8 items-center justify-center rounded-lg border border-red-200 bg-red-500/10 active:scale-90 dark:border-red-800 dark:bg-red-900/30"
+                        onClick={(event) => {
+                          event.stopPropagation();
+                          onAbortSession(runControlTarget.id, runControlTarget.__provider);
+                        }}
+                        title={t('tooltips.stopRun', { defaultValue: 'Stop active run' })}
+                      >
+                        <Square className="h-3.5 w-3.5 fill-red-600 text-red-600 dark:fill-red-400 dark:text-red-400" />
+                      </button>
+                    )}
                     <button
                       className={cn(
                         'w-8 h-8 rounded-lg flex items-center justify-center active:scale-90 transition-all duration-150 border',
@@ -352,6 +370,18 @@ export default function SidebarProjectItem({
               </>
             ) : (
               <>
+                {runControlTarget && (
+                  <div
+                    className="touch:opacity-100 flex h-6 w-6 cursor-pointer items-center justify-center rounded bg-red-50 text-red-600 opacity-100 transition-all duration-200 hover:bg-red-100 hover:text-red-700 dark:bg-red-900/20 dark:text-red-400 dark:hover:bg-red-900/40"
+                    onClick={(event) => {
+                      event.stopPropagation();
+                      onAbortSession(runControlTarget.id, runControlTarget.__provider);
+                    }}
+                    title={t('tooltips.stopRun', { defaultValue: 'Stop active run' })}
+                  >
+                    <Square className="h-3 w-3 fill-current" />
+                  </div>
+                )}
                 <div
                   className={cn(
                     'w-6 h-6 opacity-0 group-hover:opacity-100 transition-all duration-200 flex items-center justify-center rounded cursor-pointer touch:opacity-100',
@@ -411,6 +441,7 @@ export default function SidebarProjectItem({
         initialSessionsLoaded={initialSessionsLoaded}
         isLoadingSessions={isLoadingSessions}
         currentTime={currentTime}
+        processingSessions={processingSessions}
         editingSession={editingSession}
         editingSessionName={editingSessionName}
         onEditingSessionNameChange={onEditingSessionNameChange}
@@ -419,6 +450,7 @@ export default function SidebarProjectItem({
         onSaveEditingSession={onSaveEditingSession}
         onProjectSelect={onProjectSelect}
         onSessionSelect={onSessionSelect}
+        onAbortSession={onAbortSession}
         onDeleteSession={onDeleteSession}
         onLoadMoreSessions={onLoadMoreSessions}
         onNewSession={onNewSession}
diff --git a/src/components/sidebar/view/subcomponents/SidebarProjectList.tsx b/src/components/sidebar/view/subcomponents/SidebarProjectList.tsx
index a755b1d..ed15a86 100644
--- a/src/components/sidebar/view/subcomponents/SidebarProjectList.tsx
+++ b/src/components/sidebar/view/subcomponents/SidebarProjectList.tsx
@@ -22,6 +22,7 @@ export type SidebarProjectListProps = {
   loadingSessions: LoadingSessionsByProject;
   initialSessionsLoaded: Set<string>;
   currentTime: Date;
+  processingSessions: Set<string>;
   editingSession: string | null;
   editingSessionName: string;
   deletingProjects: Set<string>;
@@ -38,6 +39,7 @@ export type SidebarProjectListProps = {
   onSaveProjectName: (projectName: string) => void;
   onDeleteProject: (project: Project) => void;
   onSessionSelect: (session: SessionWithProvider, projectName: string) => void;
+  onAbortSession: (sessionId: string, provider: LLMProvider) => void;
   onDeleteSession: (
     projectName: string,
     sessionId: string,
@@ -66,6 +68,7 @@ export default function SidebarProjectList({
   loadingSessions,
   initialSessionsLoaded,
   currentTime,
+  processingSessions,
   editingSession,
   editingSessionName,
   deletingProjects,
@@ -82,6 +85,7 @@ export default function SidebarProjectList({
   onSaveProjectName,
   onDeleteProject,
   onSessionSelect,
+  onAbortSession,
   onDeleteSession,
   onLoadMoreSessions,
   onNewSession,
@@ -131,6 +135,7 @@ export default function SidebarProjectList({
               initialSessionsLoaded={initialSessionsLoaded.has(project.name)}
               isLoadingSessions={Boolean(loadingSessions[project.name])}
               currentTime={currentTime}
+              processingSessions={processingSessions}
               editingSession={editingSession}
               editingSessionName={editingSessionName}
               tasksEnabled={tasksEnabled}
@@ -144,6 +149,7 @@ export default function SidebarProjectList({
               onSaveProjectName={onSaveProjectName}
               onDeleteProject={onDeleteProject}
               onSessionSelect={onSessionSelect}
+              onAbortSession={onAbortSession}
               onDeleteSession={onDeleteSession}
               onLoadMoreSessions={onLoadMoreSessions}
               onNewSession={onNewSession}
diff --git a/src/components/sidebar/view/subcomponents/SidebarProjectSessions.tsx b/src/components/sidebar/view/subcomponents/SidebarProjectSessions.tsx
index 3091bf9..5ca0269 100644
--- a/src/components/sidebar/view/subcomponents/SidebarProjectSessions.tsx
+++ b/src/components/sidebar/view/subcomponents/SidebarProjectSessions.tsx
@@ -13,6 +13,7 @@ type SidebarProjectSessionsProps = {
   initialSessionsLoaded: boolean;
   isLoadingSessions: boolean;
   currentTime: Date;
+  processingSessions: Set<string>;
   editingSession: string | null;
   editingSessionName: string;
   onEditingSessionNameChange: (value: string) => void;
@@ -21,6 +22,7 @@ type SidebarProjectSessionsProps = {
   onSaveEditingSession: (projectName: string, sessionId: string, summary: string, provider: LLMProvider) => void;
   onProjectSelect: (project: Project) => void;
   onSessionSelect: (session: SessionWithProvider, projectName: string) => void;
+  onAbortSession: (sessionId: string, provider: LLMProvider) => void;
   onDeleteSession: (
     projectName: string,
     sessionId: string,
@@ -58,6 +60,7 @@ export default function SidebarProjectSessions({
   initialSessionsLoaded,
   isLoadingSessions,
   currentTime,
+  processingSessions,
   editingSession,
   editingSessionName,
   onEditingSessionNameChange,
@@ -66,6 +69,7 @@ export default function SidebarProjectSessions({
   onSaveEditingSession,
   onProjectSelect,
   onSessionSelect,
+  onAbortSession,
   onDeleteSession,
   onLoadMoreSessions,
   onNewSession,
@@ -117,6 +121,7 @@ export default function SidebarProjectSessions({
             session={session}
             selectedSession={selectedSession}
             currentTime={currentTime}
+            isProcessing={processingSessions.has(session.id)}
             editingSession={editingSession}
             editingSessionName={editingSessionName}
             onEditingSessionNameChange={onEditingSessionNameChange}
@@ -125,6 +130,7 @@ export default function SidebarProjectSessions({
             onSaveEditingSession={onSaveEditingSession}
             onProjectSelect={onProjectSelect}
             onSessionSelect={onSessionSelect}
+            onAbortSession={onAbortSession}
             onDeleteSession={onDeleteSession}
             t={t}
           />
diff --git a/src/components/sidebar/view/subcomponents/SidebarSessionItem.tsx b/src/components/sidebar/view/subcomponents/SidebarSessionItem.tsx
index 507478a..13b51c4 100644
--- a/src/components/sidebar/view/subcomponents/SidebarSessionItem.tsx
+++ b/src/components/sidebar/view/subcomponents/SidebarSessionItem.tsx
@@ -1,4 +1,4 @@
-import { Check, Clock, Edit2, Trash2, X } from 'lucide-react';
+import { Check, Clock, Edit2, Square, Trash2, X } from 'lucide-react';
 import type { TFunction } from 'i18next';
 import { Badge, Button } from '../../../../shared/view/ui';
 import { cn } from '../../../../lib/utils';
@@ -13,6 +13,7 @@ type SidebarSessionItemProps = {
   session: SessionWithProvider;
   selectedSession: ProjectSession | null;
   currentTime: Date;
+  isProcessing: boolean;
   editingSession: string | null;
   editingSessionName: string;
   onEditingSessionNameChange: (value: string) => void;
@@ -21,6 +22,7 @@ type SidebarSessionItemProps = {
   onSaveEditingSession: (projectName: string, sessionId: string, summary: string, provider: LLMProvider) => void;
   onProjectSelect: (project: Project) => void;
   onSessionSelect: (session: SessionWithProvider, projectName: string) => void;
+  onAbortSession: (sessionId: string, provider: LLMProvider) => void;
   onDeleteSession: (
     projectName: string,
     sessionId: string,
@@ -35,6 +37,7 @@ export default function SidebarSessionItem({
   session,
   selectedSession,
   currentTime,
+  isProcessing,
   editingSession,
   editingSessionName,
   onEditingSessionNameChange,
@@ -43,6 +46,7 @@ export default function SidebarSessionItem({
   onSaveEditingSession,
   onProjectSelect,
   onSessionSelect,
+  onAbortSession,
   onDeleteSession,
   t,
 }: SidebarSessionItemProps) {
@@ -75,7 +79,9 @@ export default function SidebarSessionItem({
           className={cn(
             'p-2 mx-3 my-0.5 rounded-md bg-card border active:scale-[0.98] transition-all duration-150 relative',
             isSelected ? 'bg-primary/5 border-primary/20' : '',
-            !isSelected && sessionView.isActive
+            !isSelected && isProcessing
+              ? 'border-sky-500/40 bg-sky-50/20 dark:bg-sky-900/10'
+              : !isSelected && sessionView.isActive
               ? 'border-green-500/30 bg-green-50/5 dark:bg-green-900/5'
               : 'border-border/30',
           )}
@@ -109,6 +115,19 @@ export default function SidebarSessionItem({
               </div>
             </div>
 
+            {isProcessing && (
+              <button
+                className="ml-1 flex h-5 w-5 items-center justify-center rounded-md bg-red-50 opacity-80 transition-transform active:scale-95 dark:bg-red-900/20"
+                onClick={(event) => {
+                  event.stopPropagation();
+                  onAbortSession(session.id, session.__provider);
+                }}
+                title={t('tooltips.stopSessionRun', { defaultValue: 'Stop this run' })}
+              >
+                <Square className="h-2.5 w-2.5 fill-red-600 text-red-600 dark:fill-red-400 dark:text-red-400" />
+              </button>
+            )}
+
             {!sessionView.isCursorSession && (
               <button
                 className="ml-1 flex h-5 w-5 items-center justify-center rounded-md bg-red-50 opacity-70 transition-transform active:scale-95 dark:bg-red-900/20"
@@ -158,7 +177,12 @@ export default function SidebarSessionItem({
           </div>
         </Button>
 
-        <div className="absolute right-2 top-1/2 flex -translate-y-1/2 transform items-center gap-1 opacity-0 transition-all duration-200 group-hover:opacity-100">
+        <div
+          className={cn(
+            'absolute right-2 top-1/2 flex -translate-y-1/2 transform items-center gap-1 transition-all duration-200',
+            isProcessing ? 'opacity-100' : 'opacity-0 group-hover:opacity-100',
+          )}
+        >
             {editingSession === session.id ? (
               <>
                 <input
@@ -200,6 +224,18 @@ export default function SidebarSessionItem({
               </>
             ) : (
               <>
+                {isProcessing && (
+                  <button
+                    className="flex h-6 w-6 items-center justify-center rounded bg-red-50 hover:bg-red-100 dark:bg-red-900/20 dark:hover:bg-red-900/40"
+                    onClick={(event) => {
+                      event.stopPropagation();
+                      onAbortSession(session.id, session.__provider);
+                    }}
+                    title={t('tooltips.stopSessionRun', { defaultValue: 'Stop this run' })}
+                  >
+                    <Square className="h-3 w-3 fill-red-600 text-red-600 dark:fill-red-400 dark:text-red-400" />
+                  </button>
+                )}
                 <button
                   className="flex h-6 w-6 items-center justify-center rounded bg-gray-50 hover:bg-gray-100 dark:bg-gray-900/20 dark:hover:bg-gray-900/40"
                   onClick={(event) => {
diff --git a/src/i18n/locales/en/sidebar.json b/src/i18n/locales/en/sidebar.json
index 7b3452c..ea02d19 100644
--- a/src/i18n/locales/en/sidebar.json
+++ b/src/i18n/locales/en/sidebar.json
@@ -44,6 +44,8 @@
     "addToFavorites": "Add to favorites",
     "removeFromFavorites": "Remove from favorites",
     "editSessionName": "Manually edit session name",
+    "stopRun": "Stop active run",
+    "stopSessionRun": "Stop this run",
     "deleteSession": "Delete this session permanently",
     "save": "Save",
     "cancel": "Cancel",
