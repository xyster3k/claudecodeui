diff --git a/server/tester-mode.js b/server/tester-mode.js
index 8538858..a56be8c 100644
--- a/server/tester-mode.js
+++ b/server/tester-mode.js
@@ -207,6 +207,58 @@ const loadBuilderArtifacts = async (projectPath, builderRunId) => {
 
 // ---- Prompt Building ----
 
+const ENVIRONMENT_SETUP_INSTRUCTIONS = [
+  '## Environment & Full System Access',
+  '',
+  'You have FULL shell access with all permissions bypassed. Use it aggressively.',
+  'You can run ANY command, install ANY package, SSH into remote servers, query databases, read logs — whatever is needed.',
+  '',
+  '### Project Instructions & Memory (READ FIRST)',
+  'Before doing anything else, read the project instructions and memory files for project-specific context:',
+  '- `CLAUDE.md` in the project root — contains SSH commands, server addresses, deploy steps, DB access, project-specific rules',
+  '- `.claude/CLAUDE.md` if it exists — additional project instructions',
+  '- `~/.claude/CLAUDE.md` — global user instructions (SSH keys, server aliases, workflow rules)',
+  '- `.claude/memory/` or `~/.claude/projects/*/memory/` — persistent memory files with learned patterns, server info, credentials locations',
+  'These files are the BEST source of truth for how to access servers, databases, and deploy for this specific project.',
+  '',
+  '### Project Runtime Environment',
+  'Before running tests or any project commands:',
+  '1. Discover the project runtime environment:',
+  '   - Python: look for `venv/`, `.venv/`, `env/`, `pyproject.toml`, `requirements.txt`, `Pipfile`, `setup.py`',
+  '   - Node.js: look for `node_modules/`, `package.json`, `yarn.lock`, `pnpm-lock.yaml`',
+  '   - Other: `Makefile`, `docker-compose.yml`, `Cargo.toml`, `go.mod`',
+  '2. **Activate the virtual environment** if one exists:',
+  '   - `source venv/bin/activate` or `source .venv/bin/activate`',
+  '   - Or use the venv Python directly: `./venv/bin/python -m pytest`',
+  '3. If dependencies are missing, **install them all**: `pip install -r requirements.txt`, `npm install`, etc.',
+  '   - Install test frameworks too if missing: `pip install pytest`, etc.',
+  '   - If there is no venv, create one: `python3 -m venv venv && source venv/bin/activate && pip install -r requirements.txt`',
+  '4. Find the correct test command: `npm test`, `pytest`, `python -m pytest`, `make test`, `cargo test`, etc.',
+  '',
+  '### Remote Server Access (SSH)',
+  'You can SSH into remote servers to check logs, verify deployments, or inspect running services.',
+  '- **First**: check `CLAUDE.md` and memory files for project-specific SSH commands and server addresses',
+  '- SSH keys are at `~/.ssh/` — check `~/.ssh/config` for configured hosts',
+  '- Also look in: `.env`, `config.py`, `deploy.sh`, `Makefile`, `docker-compose.yml`, README',
+  '- Common patterns: `ssh -i ~/.ssh/id_ed25519_deploy user@host`, or use Host aliases from ssh config',
+  '- Use SSH to: tail logs, check process status, verify endpoints, inspect config on the server',
+  '',
+  '### Database Access',
+  '- **First**: check `CLAUDE.md` and memory files for DB connection details',
+  '- Look for connection strings in: `.env`, `config.py`, `settings.py`, `database.yml`, `docker-compose.yml`',
+  '- Install DB clients if needed: `apt-get update && apt-get install -y postgresql-client` or `pip install psycopg2-binary`',
+  '- You can also SSH into the DB server and run queries there',
+  '- Use DB access to: verify data integrity, check schema, validate that the code matches the DB state',
+  '',
+  '### Rules',
+  '**NEVER report "cannot run tests" or "cannot access X" without exhausting all options first.**',
+  'If the system Python lacks packages — find or create a virtualenv and install everything.',
+  'If a DB client is missing — install it or SSH to the server that has it.',
+  'If you need to check server logs — SSH in and read them.',
+  'You have full authority to install packages, create environments, and access any system.',
+  '',
+].join('\n');
+
 const buildFeatureAnalysisPrompt = (runState) => {
   const artifacts = runState.record.builderArtifacts || {};
   const commitLogSummary = (artifacts.commitLog || [])
@@ -232,15 +284,19 @@ const buildFeatureAnalysisPrompt = (runState) => {
     '## Commit Log (changes made by builder)',
     commitLogSummary,
     '',
+    ENVIRONMENT_SETUP_INSTRUCTIONS,
+    '',
     '## Your Task',
     '',
     '1. Read the builder artifacts (PRD, plan, commit log) to understand what was built.',
-    '2. Identify all files that were changed.',
-    '3. Create a test plan with specific scenarios for each changed area.',
-    '4. Run any existing test suites (npm test, etc.) and report results.',
-    '5. Review each changed file for bugs, logic errors, edge cases, security issues.',
-    '6. Fix any bugs you find. Commit each fix with a clear message.',
-    '7. If everything passes, report success.',
+    '2. Read CLAUDE.md and memory files for project-specific server/DB/deploy info.',
+    '3. Set up the project environment (activate venv, install deps if needed).',
+    '4. Identify all files that were changed.',
+    '5. Create a test plan with specific scenarios for each changed area.',
+    '6. Run any existing test suites and report results.',
+    '7. Review each changed file for bugs, logic errors, edge cases, security issues.',
+    '8. Fix any bugs you find. Commit each fix with a clear message.',
+    '9. If everything passes, report success.',
     '',
     buildStateUpdateInstructions(runState),
   ].filter(Boolean).join('\n');
@@ -252,14 +308,18 @@ const buildGeneralDiscoveryPrompt = (runState) => [
   `Project root: ${runState.record.projectPath}`,
   `Mode: general (whole-project review)`,
   '',
+  ENVIRONMENT_SETUP_INSTRUCTIONS,
+  '',
   '## Your Task',
   '',
-  '1. Scan the project structure to understand the architecture.',
-  '2. Identify all source files, test files, configs, and entry points.',
-  '3. Run existing test suites (npm test, etc.) and collect results.',
-  '4. Review source code systematically for bugs, logic errors, security issues, performance problems.',
-  '5. Fix the most critical issues. Commit each fix with a clear message.',
-  '6. If you need to deploy to verify fixes, signal for deployment.',
+  '1. Read CLAUDE.md and memory files for project-specific server/DB/deploy info.',
+  '2. Scan the project structure to understand the architecture.',
+  '3. Set up the project environment (find and activate venv, install deps if needed).',
+  '4. Identify all source files, test files, configs, and entry points.',
+  '5. Run existing test suites and collect results.',
+  '6. Review source code systematically for bugs, logic errors, security issues, performance problems.',
+  '7. Fix the most critical issues. Commit each fix with a clear message.',
+  '8. If you need to deploy to verify fixes, signal for deployment.',
   '',
   buildStateUpdateInstructions(runState),
 ].join('\n');
@@ -278,6 +338,8 @@ const buildContinuationPrompt = (runState) => {
     `Mode: ${record.mode}`,
     `Session number: ${record.sessionCount}`,
     '',
+    ENVIRONMENT_SETUP_INSTRUCTIONS,
+    '',
     '## Previous Session Summary',
     record.sessionSummary || 'No summary from previous session.',
     '',
