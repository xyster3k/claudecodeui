diff --git a/server/routes/git.js b/server/routes/git.js
index 9d98616..b3c4e21 100755
--- a/server/routes/git.js
+++ b/server/routes/git.js
@@ -1073,6 +1073,8 @@ router.get('/remote-status', async (req, res) => {
     // Repositories initialized with `git init` can have a branch but no commits.
     // Return a non-error state so the UI can show the initial-commit workflow.
     if (!hasCommits) {
+      const authInfo = fallbackRemoteName ? await detectAuthMode(projectPath, fallbackRemoteName) : { mode: 'unknown', tokenName: null };
+
       return res.json({
         hasRemote,
         hasUpstream: false,
@@ -1080,7 +1082,9 @@ router.get('/remote-status', async (req, res) => {
         remoteName: fallbackRemoteName,
         ahead: 0,
         behind: 0,
-        isUpToDate: false,
+        isUpToDate: false,
+        authMode: authInfo.mode,
+        authTokenName: authInfo.tokenName,
         message: 'Repository has no commits yet'
       });
     }
@@ -1093,10 +1097,14 @@ router.get('/remote-status', async (req, res) => {
       remoteName = trackingBranch.split('/')[0]; // Extract remote name (e.g., "origin/main" -> "origin")
     } catch (error) {
+      const authInfo = fallbackRemoteName ? await detectAuthMode(projectPath, fallbackRemoteName) : { mode: 'unknown', tokenName: null };
+
       return res.json({
         hasRemote,
         hasUpstream: false,
         branch,
         remoteName: fallbackRemoteName,
-        message: 'No remote tracking branch configured'
+        authMode: authInfo.mode,
+        authTokenName: authInfo.tokenName,
+        message: 'No remote tracking branch configured',
       });
     }
diff --git a/src/components/git-panel/hooks/useGitPanelController.ts b/src/components/git-panel/hooks/useGitPanelController.ts
index a4b6a32..c5d7e88 100644
--- a/src/components/git-panel/hooks/useGitPanelController.ts
+++ b/src/components/git-panel/hooks/useGitPanelController.ts
@@ -423,13 +423,13 @@ export function useGitPanelController({
       const data = await readJson<GitOperationResponse>(response);
       if (data.success) {
         void fetchGitStatus();
         void fetchRemoteStatus();
         return;
       }

-      console.error('Publish failed:', data.error);
+      setOperationError(data.error ?? 'Publish failed');
     } catch (error) {
-      console.error('Error publishing branch:', error);
+      setOperationError(error instanceof Error ? error.message : 'Publish failed');
     } finally {
       setIsPublishing(false);
     }
